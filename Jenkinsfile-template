pipeline {
    agent {
        kubernetes {
            inheritFrom 'kubectl'
        }
    }
    stages {
        stage('Deploy') {
            steps {
                container('kubectl') {
                    echo "run container kubectl"
                    // kubectl apply here
                    sh "kubectl -n devops-tools get all"
                }
            }
        }
    }
}
