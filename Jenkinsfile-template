pipeline {
    agent {
        label 'kubectl'
    }
    stages {
        stage('Deploy') {
            steps {
                echo "run container kubectl"
                container('kubectl') {
                    // kubectl apply here
                    sh "kubectl -n devops-tools get all"
                }
            }
        }
    }
}
